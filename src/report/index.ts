/**
 * Report module — generates structured reports from scan results
 */

export { generateJsonReport, parseJsonReport } from './json';
export type { JsonReport, SanitizedScanResult, ReportMetadata } from './json';
